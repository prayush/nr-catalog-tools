# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: pip
    - name: Install system dependencies
      run: |
        sudo apt-get update -q
        sudo apt-get install -y -q libgsl-dev libfftw3-dev libhdf5-dev
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install pytest pre-commit
    - name: Test installation and imports
      run: |
        python -m pip install .
        python -c "import nrcats"
        python -c "import nrcats.catalog"
        python -c "import nrcats.lvc"
        python -c "import nrcats.maya"
        python -c "import nrcats.rit"
        python -c "import nrcats.sxs"
        python -c "import nrcats.utils"
        python -c "import nrcats.waveform"
    - name: pre-commit checks
      run: |
        pre-commit run --verbose --all-files --show-diff-on-failure
